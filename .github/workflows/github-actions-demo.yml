name: GitHub Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest

    defaults:
      run:
        shell: bash

    steps:
    - name: Run Tests
      run: |
        pip install -r requirements.txt
        pytest